using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bullet : MonoBehaviour
{
	public GameObject shootingStartPoint;

	void Start()
    {

	}

    // Update is called once per frame
    void Update()
    {
		//Vector3.MoveTowards(shootingStartPoint.transform.position,);
        //if (gameObject.transform.position = PlayerShooting.)
		//{
			Destroy(gameObject);
		//}
    }
}
